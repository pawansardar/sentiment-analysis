model:
  name: distilbert-base-uncased
  num_classes: 2

training:
  output_dir: ./experiments
  num_epochs: 3
  learning_rate: 0.00002   # 2e-5
  batch_size: 16
  weight_decay: 0.01
  warmup_ratio: 0.1

paths:
  model_dir: ./final_model
